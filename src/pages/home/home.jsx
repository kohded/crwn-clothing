import React from 'react';
import Directory from '../../components/directory/directory';
import './home.scss';

const Home = props => (
  <div className="home">
    <Directory />
  </div>
);

export default Home;
